type UploadFile
{
    attachmentLibrary: AdminAttachmentLibrary
    attachment: AdminAttachment
}

type Mutation
{
    adminUploadFiles(path: GraphQLString! langId: ID! files: [Upload!]!): [UploadFile]
    adminUploadFile(path: GraphQLString! langId: ID! file: Upload!): UploadFile
}
